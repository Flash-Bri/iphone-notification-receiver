# OAuth Configuration (Optional - only needed if using Manus OAuth)
EXPO_PUBLIC_OAUTH_PORTAL_URL=
EXPO_PUBLIC_OAUTH_SERVER_URL=
EXPO_PUBLIC_APP_ID=
EXPO_PUBLIC_OWNER_OPEN_ID=
EXPO_PUBLIC_OWNER_NAME=

# API Configuration (Optional - only needed if using custom API server)
EXPO_PUBLIC_API_BASE_URL=

# Note: For local development without OAuth, these can be left empty
# The app will work without these environment variables for basic ANCS functionality
